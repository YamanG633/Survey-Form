# Survey-Form
This Survey Form is a simple HTML and CSS based web form used to collect user information such as name, email, age, role, preferences, and feedback. It uses different input fields and is styled with CSS for a clean and user-friendly layout.
